require 'spec_helper'

describe OctopusSlaveNominator do
  it 'has a version number' do
    expect(OctopusSlaveNominator::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
